# 暖夕守护App

## （一）账号与绑定模块

**1. 注册与登录**

- **老人端**：手机号+验证码注册/手机号密码登录。（登录后自动记忆，长期有效）。
- **家属端**：手机号+验证码注册/手机号密码登录。

**2. 个人信息与档案管理**

- **老人端**：极简展示（超大字体展示姓名、亲切称谓、手机号）；提供需二次确认的红色退出大按钮。
- **家属端**：
  - 老人绑定：输入老人手机号 + 验证码 + 填写关系 -> 完成绑定。
  - 老人档案：性别、年龄、身高、体重、健康标签（高血压、糖尿病等）。
  - 关系设置：设置“我与老人的关系”（此称谓直接显示在老人端通知场景中）。

**3. 静默守护（高频数据缓存架构）**

- **守护预警 [开关]**：默认关闭。预警阈值：12小时/24小时/48小时。
- **判定逻辑**：系统监测老人端的“基础应用交互行为”（如打开App、点击任意按钮）均视为“活跃”。
- **技术架构（核心考核点）**：前端触发活跃动作后，后端先更新 Redis 缓存缓冲，仅每隔 5-10 分钟或在关键节点将“最后活跃时间”异步同步写入 MySQL 数据库，防止高频操作压垮数据库。
- **免打扰时段**：默认 22:00 - 06:00（支持家属自定义）。

## （二）血压记录模块

**1. 老人端**

- **提醒机制**：到达预设时间，自动唤醒屏幕。
- **语音播报**：播放系统 TTS（文本转语音）标准提示音：“[称谓]，时间到了，请测量您的[血压]”。
- **视觉弹窗**：极简全屏弹窗，大图标 + “测量提醒” + 两个大按钮（“去记录”、“稍后”）。
- **数据录入**：蓝牙连接esp32-c3检测结果自动输入（设定固定按键方式对应数值情况）短按单击高，短按双击低，长按正常，前端配合基础格式与合理值校验。
- **预警提示**：提交后弹窗+语音提示状态结果：正常/偏高/偏低。

**2. 家属端**

- **提醒设置**：设置测量类型（血压）+ 时间。
- **数据监控**：查看完整历史记录列表/曲线，以及参考范围对比。
- **同步与通知**：老人录入完成后，家属端首页数据实时刷新；若数值异常，触发“健康警报”通知。

**3. 硬件端（基于 ESP32-C3 的模拟采集终端）**

- **极简交互设计**：通过 ESP32-C3 开发板上的物理按键（或外接微动开关）蓝牙连接app模拟健康数据的采集过程。
  - **单击按键**：模拟触发“数据偏高”（例如：血压 160/100）。
  - **双击按键**：模拟触发“数据偏低”（例如：血糖 3.0）。
  - **长按按键**：模拟触发“数据正常”（例如：血压 120/80）。
- **数据上云**：触发按键后，ESP32-C3 读取代码中预设好的模拟数据（JSON 格式），通过蓝牙上传
- **状态反馈**：通过开发板上的 LED 灯提供直观的交互反馈（如：按下时蓝灯闪烁代表正在上传，上传成功且异常时红灯长亮，正常则绿灯长亮）。

## （三）用药提醒与库存管理模块

**1. 老人端**

- **提醒触发**：播放「标准慢节奏铃声」+「系统TTS语音播报药名与剂量」。
- **智能聚合弹窗**：同一时间点的多种药物合并为一次弹窗，列表/平铺展示药品名称与剂量。
- **操作确认**：保留“全部已服”（绿色大按钮）和“稍后提醒”（灰色大按钮）。点击“全部已服”后，后端执行库存扣减。

**2. 家属端**

- **设置流程**：输入药名 + 剂量 + **当前库存量（如20粒） + 预警阈值（如6粒）** + 服用时间与周期。
- **状态监控**：首页看板实时显示今日服药状态。
- **库存管理（核心业务闭环）**：
  - **自动扣减**：老人点击确认后，后台执行库存 `-1` 操作（需在后端处理好并发更新时的锁机制，确保数据一致性）。
  - **低量提醒**：库存低于阈值时，家属端显示红色“缺药补货”标签，并推送备药提醒通知。

## （四）定位防走失模块

**1. 老人端**

- **紧急呼叫**：首页设置红色大按钮「紧急呼叫」。
- **执行逻辑**：点击后 -> 步骤1：获取当前经纬度并异步上传后端 -> 步骤2：调用系统拨号盘拨通首位紧急联系人。
- **后端分发**：后端接收到经纬度后，调用第三方逆地理编码API（如高德/百度API）将其转化为具体的文本地址（如“XX路XX号”），并向所有家属推送App内通知。

**2. 家属端**

- **位置查看**：以“文字地址 + 时间戳”的列表形式展示老人的求救位置记录，省去复杂的地图组件渲染。
- **联系人设置**：添加3-5名紧急联系人并设置优先级排序。
- **数据维护（定时任务考点）**：后端配置 Spring 定时清理任务（Cron Job），系统仅保留最近 7 天的紧急呼叫记录与定位快照，过期数据自动硬删除或归档。

## （五）AI心理关怀模块

**1. 老人端（极简交互）**

- **入口**：首页“暖心聊天”大按钮。
- **交互方式（按住说话）**：采用类似微信语音的交互。老人按住屏幕大按钮开始录音，松手后发送。
- **状态提示**：发送后屏幕显示“思考中...”，后端调用语音转文字（STT）-> 大模型API获取回复 -> 文字转语音（TTS）。
- **语音播报**：获取到音频或文本后，前端自动播放 AI 的语音回复内容，并在屏幕上展示超大字体的文字版回复。
- **一键重听**：底部常驻“再听一遍”按钮，方便老人重复收听当前回复。

